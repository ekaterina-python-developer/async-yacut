# YaCut - Сервис укорачивания ссылок

## Описание проекта

YaCut — это веб-сервис для укорачивания длинных ссылок и загрузки файлов на Яндекс.Диск. Сервис предоставляет короткие удобные ссылки вместо длинных URL-адресов и позволяет загружать файлы с генерацией коротких ссылок для доступа к ним.

## Основные функции

### 📍 Укорачивание ссылок
- Преобразование длинных URL в короткие ссылки
- Возможность указания пользовательского варианта короткой ссылки
- Автоматическая генерация коротких ссылок (6 случайных символов)
- Валидация вводимых данных

### 📁 Загрузка файлов
- Асинхронная загрузка нескольких файлов на Яндекс.Диск
- Автоматическая генерация коротких ссылок для загруженных файлов
- Поддержка различных форматов файлов (изображения, документы, текстовые файлы)

### 🔗 API для разработчиков
- REST API для создания и получения коротких ссылок
- JSON-формат запросов и ответов
- Документация в формате OpenAPI

## Технологии

- **Backend**: Flask, Python 3.12+
- **База данных**: SQLite (разработка), SQLAlchemy ORM
- **Асинхронные запросы**: aiohttp
- **Формы**: Flask-WTF, WTForms
- **Фронтенд**: HTML, Bootstrap, Jinja2
- **Хранилище файлов**: Яндекс.Диск API

## Установка и запуск

### Предварительные требования
- Python 3.12 или выше
- Аккаунт на Яндекс.Диске
- OAuth-токен для Яндекс.Диск API

### Шаги установки

Клонировать репозиторий и перейти в него в командной строке:

```
git clone 
```

```
cd yacut
```

Cоздать и активировать виртуальное окружение:

```
python3 -m venv venv
```

* Если у вас Linux/macOS

    ```
    source venv/bin/activate
    ```

* Если у вас windows

    ```
    source venv/scripts/activate
    ```

Установить зависимости из файла requirements.txt:

```
python3 -m pip install --upgrade pip
```

```
pip install -r requirements.txt
```

Создать в директории проекта файл .env с четыремя переменными окружения:

```
FLASK_APP=yacut
FLASK_ENV=development
SECRET_KEY=your_secret_key
DB=sqlite:///db.sqlite3

DISK_TOKEN=your_token_yandex_disk

FLASK_DEBUG=1

```

Создать базу данных и применить миграции:

```
flask db init
flask db migrate
flask db upgrade
```

Запустить проект:

```
flask run
```

После запуска проект будет доступен по адресу: http://127.0.0.1:5000

## Использование

### Веб-интерфейс

#### Главная страница (/)

Введите длинную ссылку в поле "Длинная ссылка"

При желании укажите свой вариант короткой ссылки (до 16 символов)

Нажмите "Создать ссылку"


#### Страница загрузки файлов (/files)

Выберите один или несколько файлов для загрузки

Поддерживаемые форматы: JPG, PNG, GIF, PDF, DOC, DOCX, TXT

Нажмите "Загрузить файлы"

После загрузки появятся короткие ссылки для скачивания

### API

Создание короткой ссылки

```
http
POST /api/id/
Content-Type: application/json

{
  "url": "https://example.com/very-long-url",
  "custom_id": "my-link"
}
```

Получение оригинальной ссылки

```
http
GET /api/id/{short_id}/
```

## Автор
[Екатерина воронова](https://github.com/katya-voronova)
